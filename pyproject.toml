[project]
name = "movielens-research"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "pandas",
    "numpy",
    "scikit-learn",
    "scipy",
]

[[tool.uv.index]]
url = "https://pypi.org/simple/"
default = true
